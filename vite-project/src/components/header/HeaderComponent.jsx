import { NavComponent } from "../nav/NavComponent.jsx";

export const HeaderComponent = () => {
    return (
        <header>
            <h1>Header</h1>
            <NavComponent />
        </header>
    );
}